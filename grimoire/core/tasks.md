# Tasks - global register

> Always in context - loaded together with core/. The user writes this file; the engine only reads it.
> The engine never writes here on its own: it does not invent, propose, or auto-append.
> Line format: `- [ ] short action -> [[mem_<sub>]]` (link to the subtopic where the detail lives).

---
